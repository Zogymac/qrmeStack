import styles from './Header.module.scss';
const Header = () => {
  return (
        <div className={styles.Header}>
            <h1 className={styles.Header_bar}>Header</h1>
        </div>
        )
}

export default Header